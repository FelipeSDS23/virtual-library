import styles from "./Header.module.css";

const Header = () => {
    return (
        <header className={styles.teste}>
            <h1>Meu Site</h1>
        </header>
    );
};

export default Header;